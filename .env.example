# Dad's Interview Bot environment template
# Fill every value with a real secret before deploying. Empty strings are treated as missing and will crash the app.

# REQUIRED: select the blob storage backend.
#   - Use "memory" for local testing only (data resets on restart).
#   - Use "supabase" in production once SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, and SUPABASE_STORAGE_BUCKET are set.
STORAGE_MODE=

# REQUIRED when STORAGE_MODE="supabase". Values must come from Supabase → Project Settings → API.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=

# Optional extras referenced throughout the app. Uncomment and populate as needed.
# SUPABASE_ANON_KEY=
# OPENAI_API_KEY=
# RESEND_API_KEY=
# DEFAULT_NOTIFY_EMAIL=
